# Sample env file, replace with actual values (./backend/.env)
POSTGRES_USER=appuser
POSTGRES_PASSWORD=changeme
POSTGRES_DB=appdb

# Connection string for Express
DATABASE_URL=postgresql://appuser:changeme@localhost:5432/appdb