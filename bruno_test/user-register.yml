info:
  name: user-register
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/users/register"
  body:
    type: json
    data: |-
      {
        "email": "john.doed4d@example.com",
        "firstName": "John",
        "lastName": "Doe",
        "password": "P4$$word"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
